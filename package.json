{
  "name": "ffprobe-client",
  "description": "A zero-dependency, promise-based Node.js API for ffprobe",
  "version": "0.0.0-development",
  "main": "index.js",
  "license": "MIT",
  "author": "Philip Scott <pscott@zeptohost.com>",
  "keywords": [
    "ffmpeg",
    "ffprobe",
    "video",
    "audio",
    "stream",
    "media",
    "json"
  ],
  "scripts": {
    "fix": "standard --fix",
    "lint": "standard",
    "test": "yarn run lint && sudo ./scripts/gen-output.sh && nyc mocha --reporter spec --timeout 10000",
    "semantic-release": "semantic-release",
    "travis-deploy-once": "travis-deploy-once"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ScottyFillups/ffprobe-client.git"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "dirty-chai": "^2.0.1",
    "mocha": "^5.2.0",
    "nock": "^9.6.1",
    "nyc": "^12.0.2",
    "standard": "^11.0.1",
    "semantic-release": "^15.9.9",
    "travis-deploy-once": "^5.0.2"
  }
}
